# Metadata for data sources for the covariate download and extraction functions.

Metadata for data sources for the covariate download and extraction
functions.

## Usage

``` r
nc_covariate_table()
```

## Value

`data.frame` containing information on the resolution and source for the
covariate data available through `naturecounts`.
